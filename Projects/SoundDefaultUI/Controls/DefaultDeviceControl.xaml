<UserControl x:Class="SoundDefaultUI.Controls.DefaultDeviceControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:SoundDefaultUI"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:DefaultDeviceViewModel}">
    <UserControl.Resources>
        <Style x:Key="GrayOnDisabledTextBlock" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}" />
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBodyDisabled}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Border
        Margin="25,0,18,0"
        Background="Transparent"
        BorderBrush="{DynamicResource MaterialDesignDivider}"
        BorderThickness="2"
        CornerRadius="6"
        materialDesign:ColorZoneAssist.Mode="PrimaryMid"
        Opacity="{Binding Availability2GroupOpacity}"
        Padding="10">
        <StackPanel>
            <TextBlock Text="PnP Identifier" FontWeight="Bold" Margin="0,0,0,5"
                       IsEnabled="{Binding IsDeviceNotNull}"
                       Style="{DynamicResource GrayOnDisabledTextBlock}" />

            <TextBox Text="{Binding Device.PnpId, Mode=OneWay, TargetNullValue='null value', FallbackValue=''}"
                     IsReadOnly="True" Margin="0,0,0,10"
                     IsEnabled="{Binding IsDeviceNotNull}" />

            <TextBlock Text="Name" FontWeight="Bold" Margin="0,0,0,5"
                       IsEnabled="{Binding IsDeviceNotNull}"
                       Style="{DynamicResource GrayOnDisabledTextBlock}" />

            <TextBox Text="{Binding Device.DeviceName, Mode=OneWay, TargetNullValue='null value', FallbackValue=''}"
                     IsReadOnly="True" Margin="0,0,0,10"
                     IsEnabled="{Binding IsDeviceNotNull}" />

            <TextBlock Text="Output Volume Level" FontWeight="Bold" Margin="0,0,0,5"
                       IsEnabled="{Binding IsRenderingAvailable}"
                       Style="{DynamicResource GrayOnDisabledTextBlock}" />
            <Grid Height="28" Margin="0,0,0,10" VerticalAlignment="Center">
                <ProgressBar Value="{Binding Device.RenderVolumeLevel, Mode=OneWay, TargetNullValue=0.0, FallbackValue=0.0}"
                             Maximum="1000" Height="28" VerticalAlignment="Center"
                             Opacity="{Binding RenderingAvailability2IndicatorOpacity}" />
                <TextBlock Text="{Binding Device.RenderVolumePercentText, Mode=OneWay, TargetNullValue='-', FallbackValue='-'}"
                           HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"
                           Foreground="{DynamicResource MaterialDesignBody}" />
            </Grid>

            <TextBlock Text="Input Volume Level" FontWeight="Bold" Margin="0,0,0,5"
                       IsEnabled="{Binding IsCapturingAvailable}"
                       Style="{DynamicResource GrayOnDisabledTextBlock}" />
            <Grid Height="28" Margin="0,0,0,10" VerticalAlignment="Center">
                <ProgressBar Value="{Binding Device.CaptureVolumeLevel, Mode=OneWay, TargetNullValue=0.0, FallbackValue=0.0}"
                             Maximum="1000" Height="28" VerticalAlignment="Center"
                             Opacity="{Binding CapturingAvailability2IndicatorOpacity}" />
                <TextBlock Text="{Binding Device.CaptureVolumePercentText, Mode=OneWay, TargetNullValue='-', FallbackValue='-'}"
                           HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"
                           Foreground="{DynamicResource MaterialDesignBody}" />
            </Grid>
        </StackPanel>
    </Border>
</UserControl>